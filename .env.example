# Database Configuration
# In production, use the actual connection string provided by your hosting provider
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/workreport"

# JWT Secret
# Generate a secure random string (e.g., `openssl rand -base64 32`)
# NEXTAUTH_SECRET is used by the app, JWT_SECRET is a fallback
NEXTAUTH_SECRET="change-this-to-a-secure-random-secret-in-production"
JWT_SECRET="change-this-to-a-secure-random-secret-in-production"

# Application URL
# In production, set this to your domain (e.g., https://workreport.example.com)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Email Configuration (for password resets)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password
# EMAIL_FROM=noreply@example.com
# EMAIL_FROM_NAME="Work Report App"

# Optional: Google OAuth Configuration
# GOOGLE_CLIENT_ID=your_client_id
# GOOGLE_CLIENT_SECRET=your_client_secret
# GOOGLE_ALLOWED_DOMAINS=example.com
